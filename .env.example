# ─── Flask ────────────────────────────────────────────────────────────────────
SECRET_KEY=change-me-to-a-long-random-string
FLASK_DEBUG=true

# ─── Integration Database ─────────────────────────────────────────────────────
DATABASE_URL=sqlite:////app/data/integration.db

# ─── OpenEMR Admin ────────────────────────────────────────────────────────────
OPENEMR_ADMIN_USER=admin
OPENEMR_ADMIN_PASS=change-me-admin

# ─── MariaDB for OpenEMR ──────────────────────────────────────────────────────
MYSQL_ROOT_PASSWORD=change-me-db-root
OPENEMR_DB_USER=openemr
OPENEMR_DB_PASS=change-me-db-openemr

# ─── OpenEMR URLs ─────────────────────────────────────────────────────────────
# The public HTTPS URL of OpenEMR 
# Used as the OAuth2 issuer URL (OPENEMR_SETTING_site_addr_oath).
# Must match exactly what Flask uses when validating OpenEMR tokens.
OPENEMR_PUBLIC_URL=https://openemr-public-domain.com

# The CLIENT ID issued by OpenEMR after you register the app 
OPENEMR_CLIENT_ID=

# Note: OPENEMR_BASE_URL and OPENEMR_FHIR_BASE_URL are set directly in

# ─── Zoom ─────────────────────────────────────────────────────────────────────
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
ZOOM_WEBHOOK_SECRET_TOKEN=

# ─── JWKS / RSA Keys ──────────────────────────────────────────────────────────
PRIVATE_KEY_PATH=/app/keys/private.pem
PUBLIC_KEY_PATH=/app/keys/public.pem
KEY_ID=openemr-integration-key-1

# ─── Public-facing URL of the Flask app ───────────────────────────────────────
# The public HTTPS URL NPM proxies to port 5000.
# Used to construct the JWKS URL: ${PUBLIC_BASE_URL}/.well-known/jwks.json
# This URL gets registered in OpenEMR as the jwks_uri for your app.
PUBLIC_BASE_URL=https://flask-integration-public-domain.com

# ── Logging ────────────────────────────────────────────
LOG_LEVEL=DEBUG
LOG_FILE=./logs/log.log