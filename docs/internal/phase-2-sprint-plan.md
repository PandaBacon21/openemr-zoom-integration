# Phase 2 Sprint Plan

This is a future-facing planning reference for Phase 2 work. Status, size, priority, and notes mirror the source planning sheet; it is not a statement that the backlog items are currently implemented.

## Sprint 7 - Stabilization And Staging Readiness

| ID    | Area           | Size | Status  | Priority | Story / Task                                   | Notes                                                                                                                                                                                                      |
| ----- | -------------- | ---: | ------- | -------- | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S7-01 | Flask Backend  |    S | Done    | High     | Manual arrival duplicate encounter fix         | `find_encounter_for_appointment()` carries the guarded `external_id IS NULL` fallback and now returns `(encounter_number, source)`; the async processor writes an `encounter.claimed` audit when the manual-fallback path matches (c3eba07).                                                                              |
| S7-02 | Flask Backend  |    S | Done    | Medium   | `AppointmentTypeFilter.zoom_account_id` FK fix | Verified the FK references `ZoomAccount.account_id` as a `String(128)` PK in both [models.py](../../server/app/models.py) and migration `5ecd2a942ca3`. Already satisfied by the Sprint 6 refactor (92fd3e1); backlog entry resolved without code change.                                                              |
| S7-03 | Flask Backend  |    M | Done    | High     | Note writeback mode bug                        | Verified `write_note_to_encounter()` at `services/openemr/note.py` correctly invokes both `_upsert_soap_form` and `_upsert_clinical_note_form` when `note_writeback_mode == "both"`; no skip behavior present. Backlog entry resolved without code change.                                                                |
| S7-04 | Flask Backend  |    S | Done    | High     | `_fetch_note_with_retry` HTTPError handling    | `requests.exceptions.HTTPError` caught inside the retry loop with status-code logging, `note.fetch_error` audit, and giveup/retry logic intact (c8f313c).                                                                                                                                                              |
| S7-05 | Flask Backend  |    M | Done    | High     | Redesign OpenEMR verify flow                   | `register_zoom_account` flips `oauth_clients.is_enabled = 1` via direct DB UPDATE immediately after RFC 7591 registration. Manual "Enable Client" admin step removed from the SE flow; 5-min polling scheduler + force-refresh token churn dissolved. Failed enable rolls back full registration (deregister + keypair cleanup). New audit events: `openemr.client_enabled`, `openemr.client_enable_failed`. |
| S7-06 | React Frontend |    S | Done    | High     | `runVerify` race condition                     | `AccountDetail.runVerify` now compares against `lastCheckedAccountId.current` after the await and drops stale responses in both success and catch branches. Also removed the 60s polling loop (dead code post auto-enable from S7-05) and refreshed two pieces of stale UI text. The chip label `"OpenEMR Not Enabled"` → `"OpenEMR Unverified"` and the Alert fallback was rewritten to match the auto-enable reality. |
| S7-07 | Flask Backend  |    S | Done    | Medium   | Stop token minting on every verify poll        | Dissolved by S7-05: the polling loop (`check_pending_registrations` / `trigger_verification_scheduler`) was deleted. `verify_openemr_token_for_account` no longer passes `force_refresh=True`; it uses the cached token like every other call path. |
| S7-08 | OpenEMR        |    M | Done    | High     | Multi-client JWT kid lookup fix                | Root cause was an OpenEMR PHP bug, not JWKS caching (OpenEMR doesn't cache JWKS). `RsaSha384Signer::verify()` reads kid from `$this->headers['kid']`, which is only set during signing — during verification it's always null, so `JsonWebKeySet::getJSONWebKey()` returned the first RSA key in the JWKS array regardless of which client's token was being verified. Patched in `openemr/patches/RsaSha384Signer.php` to extract kid from the JWT header bytes in `$payload`. Baked into the custom OpenEMR image via `openemr/Dockerfile`. Confirmed working: both accounts verify cleanly with their correct kids logged. |
| S7-09 | Infrastructure |    S | Done    | Medium   | `docker-compose.staging.yml` overlay           | Created at repo root. Minimal override that extends OpenEMR healthcheck `start_period` from 6m → 15m so the `zoom-module-init` helper (which `depends_on: openemr: service_healthy`) actually fires on slower Proxmox hardware. Applied via `docker compose -f docker-compose.yml -f docker-compose.staging.yml ...` or transparently by `start-staging.sh`. |
| S7-10 | Infrastructure |    S | Done    | Medium   | `start-staging.sh`                             | Staging analog of `start-dev.sh`. Identical body except: (1) layers in the S7-09 overlay, (2) runs `alembic upgrade head` at the end so migrations apply automatically (dev users run them manually). `git pull` and `docker compose build --no-cache` are intentionally kept outside the script — they are deploy steps, not "start the stack" steps. CLAUDE.md and implementation-setup-guide.md updated to point at the new script. |
| S7-11 | Flask Backend  |    S | Done    | High     | Tenant ID exposure                             | `tenant_id` was added to `GET /config/registrations` and displayed in `CredentialsSection.tsx`.                                                                                                            |
| S7-12 | QA             |    M | Backlog | High     | End-to-end staging regression                  | Exercise appointment -> waiting room -> meeting started -> note created -> SOAP and clinical note writeback; verify both accounts resolve correctly.                                                       |
| S7-13 | React Frontend |    S | Done    | Medium   | Config + Dashboard UI polish                   | Two UI improvements bundled. (1) **Provider mappings table** — surface the OpenEMR user ID (matching the value used in audit logs) as a new column. The current model stores this on `UserMapping.openemr_user_id`. (2) **Dashboard audit log filtering** — token-fetch noise (newly added `jwks.fetched`, `openemr.token.verified`, `zoom.token.verified`) dominates the view on busy days. Add a toggle that hides those event types by default but always shows them when `success=false`; toggle off reveals all. Backend gains an `exclude_event_types` query param on `GET /audit/logs` applied as `WHERE NOT (event_type IN (...) AND success = true)`. Audit-event taxonomy in `services/audit.py` docstring should be updated to include the new token events. **Implementation:** Both improvements shipped together along with auth/JWKS instrumentation. Backend now emits `jwks.fetched`, `openemr.token_refresh_failed`, `openemr.token_verify_*`, `zoom.token_refresh_failed`, and registration-time `zoom.credentials_validated` / `zoom.credentials_validation_failed` events; `services/audit.py` docstring updated to cover them. `GET /audit/logs` gained the `exclude_event_types` query param applied per the planned `WHERE NOT (...)` shape. User ID column added to AccountProvidersTab. Dashboard chip toggle hides token-fetch noise by default; always shows failed rows. (ae3ec6d) |
| S7-14 | React Frontend |    M | Done    | Low      | Provider mappings: facility column             | Denormalized facility fields via Alembic migration `a3f4e1c92b04` while the table was still provider-specific; the current `UserMapping` provider-role rows carry `openemr_facility_id` (Integer, nullable) and `openemr_facility_name` (String(255), nullable). `_normalize_practitioner` extends the existing `users` lookup from `SELECT id` → `SELECT id, facility, facility_id`, returning facility in the practitioner dict. `_create_provider_mapping`, `POST /config/providers`, and `GET /config/providers` all thread facility through. React `AccountProvidersTab` adds a "Facility" column with an italic em-dash placeholder for pre-S7-14 mappings. |

## Sprint 8 - Internal Database Access  *(Done — single commit `f62b730`)*

| ID    | Area           | Size | Status  | Priority | Story / Task                     | Notes                                                                                                  |
| ----- | -------------- | ---: | ------- | -------- | -------------------------------- | ------------------------------------------------------------------------------------------------------ |
| S8-01 | Infrastructure |    S | Done    | High     | Add DbGate Docker service        | Internal-only `dbgate/dbgate`, no NPM exposure; connect to both `mariadb-emr` and `zoomly-postgres`. **Implementation:** DbGate service added to `docker-compose.yml` on `integration_net` (internal-only, no port mapping to host). Connects to both databases by name. (f62b730) |
| S8-02 | React Frontend |    S | Done    | High     | DbGate nav item                  | Add an admin-only Database nav item that renders DbGate in a full-height iframe through Flask.       **Implementation:** `DatabasePage.tsx` renders DbGate inside a full-height iframe pointed at `/admin/db` (proxied through Flask). Nav item added to `Layout.tsx`. (f62b730) |
| S8-03 | Flask Backend  |    S | Done    | Medium   | DbGate reverse proxy             | Add JWT-protected `/admin/db` proxy to `http://dbgate:3000` so DbGate stays off the public internet. **Implementation:** `blueprints/admin/` reverse-proxies `/admin/db` and `/admin/db/<path>` to DbGate via `http://dbgate:3000`. Auth path is `verify_jwt_cookie_or_header()` (iframe-friendly cookie auth) — the JWT `admin_token` cookie is set on login alongside the response body so the iframe carries the auth context. (f62b730) |
| S8-04 | QA             |    S | Done    | Medium   | Validate DbGate database access  | Verify browsing and read/write operations for both OpenEMR MariaDB and Zoomly PostgreSQL schemas.    **Implementation:** Verified browsing + r/w against both `mariadb-emr` and `zoomly-postgres` connections inside the embedded DbGate UI. (f62b730) |

## Sprint 9 - SE Demo Completion  *(Carried to Sprint 13 — see bottom of file)*

> Sprint 9 was deferred so the demo data buildout (Sprint 12) could land first — running an SE walkthrough against the original 30-patient seed wouldn't have exercised the multi-facility/multi-persona flows that Sprint 12 enables. The four stories below were renumbered into Sprint 13. They are retained here for ID continuity and historical reference.

| ID    | Area          | Size | Status      | Priority | Story / Task                             | Notes                                                                                                                            |
| ----- | ------------- | ---: | ----------- | -------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| S9-01 | QA            |    L | Done | High     | SE demo run-through                      | Carried to S13-01 and completed there. SE testers can run the full telehealth demo workflow independently against the Sprint 12 dataset.                 |
| S9-02 | OpenEMR       |    M | Done | High     | Wire `complete_zoom_note` to eSign event | Carried to S13-02 and completed there. eSign event re-wired in `openemr/patches/clinical_note_fetcher/forms.php` using `window.location.origin` to dodge the 301 redirect. |
| S9-03 | Flask Backend |    M | Done | Medium   | Locked/eSigned encounter guard           | Carried to S13-03 and completed there. `encounter_lock_target()` queries `esign_signatures` for both `form_encounter` and `forms` locks; SOAP/Clinical Notes writeback refuses on hit. |
| S9-04 | QA            |    M | Done | Medium   | Demo script documentation                | Carried to S13-04 and completed there. SE walkthrough scripted against the Sprint 12 personas (CHR / GER / BH / SUD / HYA / NEW). |

## Sprint 10 - Epic-Style OpenEMR Skin And Custom Image

| ID     | Area           | Size | Status  | Priority | Story / Task                           | Notes                                                                                                              |
| ------ | -------------- | ---: | ------- | -------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| S10-01 | Research       |    M | Backlog | High     | Research Epic EHR UI patterns          | Document patient banner, left nav, chart sidebar, top toolbar, color palette, and layout conventions.              |
| S10-02 | Infrastructure |    L | Done    | High     | Custom OpenEMR Docker image foundation | **Done.** `openemr/Dockerfile` builds `zoomly-openemr:local` from `openemr/openemr:8.0.0`, baking in every PHP patch and branding asset with `apache:apache` + `644`. `docker-compose.yml` references the build directly; openemr/patches/branding bind mounts removed from the base compose, `branding` init container deleted, `openemr_public` volume dropped. The dev override (`docker-compose.override.yml`, with a committed `.example` template) optionally re-mounts `openemr/patches/` for fast iteration; `start-dev.sh --baked` bypasses it to test the baked image. `start-staging.sh` chmod block removed; new `start-prod.sh` mirrors staging without DbGate and overlays. |
| S10-03 | Infrastructure |    M | Backlog | Medium   | Image registry pipeline                | Push the custom OpenEMR image to the Zoom-internal GitLab container registry (instead of GHCR per the project move); update `docker-compose.yml` to swap `build:` for `image: <registry>/zoomly-openemr:<sha>` in staging/prod. Tag strategy + CI integration TBD with the GitLab move.   |
| S10-04 | OpenEMR        |    L | Backlog | High     | Epic-style patient banner              | Override patient header with patient name, DOB/age/sex, MRN, allergies, and insurance summary.                     |
| S10-05 | OpenEMR        |    L | Backlog | High     | Epic-style left navigation             | Convert horizontal top navigation into a persistent left rail with chart modules and active state.                 |
| S10-06 | OpenEMR        |    L | Backlog | High     | Epic-style chart sidebar               | Split encounter/chart view into chronological encounter list plus selected encounter content.                      |
| S10-07 | OpenEMR        |    M | Backlog | Medium   | Epic color palette and typography      | Add global CSS for dark navy rail, white content area, teal accents, gray table rows, and utilitarian type.        |
| S10-08 | OpenEMR        |    M | Backlog | Medium   | Epic-style top toolbar                 | Add patient search, user context, and notifications; remove OpenEMR-specific branding elements.                    |
| S10-09 | QA             |    M | Backlog | Medium   | End-to-end visual QA                   | Walk the full demo flow in the Epic-skinned OpenEMR and check cross-page layout behavior.                          |

## Sprint 11 - ZCC CTI Screen Pop

Status note: this section started as backlog planning, but a substantial
Epic-style ZCC CTI implementation now exists behind `ENABLE_EPIC_ZCC`.
Implemented pieces include account-scoped OAuth/JWKS, PatientLookUp,
Practitioner.Search, ReceiveCommunication3 screen-pop dispatch, OpenEMR
screen-pop SSE bootstrap/streaming, ZCC user lookup, admin configuration UI,
and outbound initiate-call/click-to-dial plumbing.

| ID     | Area           | Size | Status  | Priority | Story / Task                                | Notes                                                                                                               |
| ------ | -------------- | ---: | ------- | -------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| S11-01 | Research       |    M | Backlog | High     | Research ZCC Smart Embed and screen pop API | Document ZCC caller context delivery, payload structure, trigger timing, and Smart Embed iframe/deep-link APIs.     |
| S11-02 | Database       |    M | Done    | High     | `AccountConfig` CTI schema additions        | Implemented as Epic-ZCC-specific `AccountConfig.epic_zcc_*` columns plus persisted bearer-token cache fields on `ZoomAccount`. |
| S11-03 | Flask Backend  |    L | Done    | High     | ZCC CTI middleware endpoint                 | Implemented as Epic-shaped PatientLookUp + ReceiveCommunication3 + SSE screen-pop routes under `/zoomly/<account>/interconnect-amcurprd-oauth`; outbound click-to-dial routes through ZCC initiate-call and preloads patient context into the lookup cache. |
| S11-04 | Flask Backend  |    L | Backlog | High     | FHIR patient lookup service                 | Implement lookup functions for phone, DOB, last4 SSN, and patient ID, composed by the configured match chain.       |
| S11-05 | Flask Backend  |    M | Done    | Medium   | Provider match lookup                       | Agent/provider identity now resolves through `UserMapping` role flags, including `is_zcc_agent`, `zcc_user_id`, and `openemr_user_id`. |
| S11-06 | Flask Backend  |    S | Done    | Medium   | OpenEMR deep-link URL builder               | Implemented through OpenEMR-side SSE navigation events that open the patient chart when matched or patient finder when no exact match is available. |
| S11-07 | Research       |    M | Backlog | High     | ZCC Flow configuration                      | Document inbound call flow setup, caller ANI capture, `/cti/screenpop` trigger, and trigger timing.                 |
| S11-08 | Flask Backend  |    S | Backlog | Medium   | No-match screen pop                         | Return new patient URL when all match fields fail: `{OPENEMR_PUBLIC_URL}/interface/patient_file/patient_file.php`.  |
| S11-09 | React Frontend |    M | Done    | Medium   | CTI configuration UI                        | Implemented as the feature-gated Epic ZCC account tab for enablement, credential initialization, generated URLs, ZCC backend, and Zoom Admin Portal field capture. |
| S11-10 | QA             |    L | Backlog | High     | End-to-end CTI test                         | Simulate inbound ZCC call through Flow, middleware lookup, patient match/no-match, and Smart Embed screen pop.      |
| S11-11 | OpenEMR        |    S | Done    | High     | Guard OpenEMR ZCC click-to-call controls    | OpenEMR callbar/subscriber and phone-link click-to-call controls render only for sessions whose screen-pop bootstrap returns active ZCC-agent streams. Non-ZCC users see plain phone numbers in demographics, patient finder, and appointment-card contexts. Shared phone-link helper also suppresses demo seed numbers ending in `555-####` so synthetic demo phones are not dialed through ZCC. |

## Sprint 12 - Demo Data Enrichment  *(Done — commits `529275f`, `fd80fd2`, `e52e654`, `896b936`, plus seed-structure follow-ups `af6c6e8` / `cd01b15` / `19b56d1`)*

Telehealth-focused demo data refresh. Every persona, every appointment category, and every clinical record is chosen to make sense as a Zoom-managed visit. Conditions skew toward what a clinician actually manages over video — behavioral health, chronic disease stable follow-up, cardiology post-event monitoring, MAT — and away from acute presentations that need in-person exam or imaging.

**Note on file references:** Every story below refers to `demo_data.sql` because that's how the work was scoped at planning time. During implementation that single file was split into seven ordered files (`01_globals.sql` → `07_clinical_data.sql`); see `af6c6e8`. The plan-text references are preserved as historical record — current locations are documented in `CLAUDE.md` and the README of `seed_data/`.

| Story ID | Sprint    | Story / Task                          | Component | Size | Status  | Priority | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------- | --------- | ------------------------------------- | --------- | ---: | ------- | -------- | -------- |
| S12-01   | Sprint 12 | Persona matrix design                 | Seed Data |    S | Done    | High     | Lock in the per-PID persona assignment as a telehealth-skewed matrix (PIDs 100–129 → PSY-S severe psychiatric / BH-PC behavioral health in primary care / CHR chronic disease stable follow-up / CV-F cardiology follow-up / GER geriatric polypharmacy w/ caregiver-assist / SUD substance use MAT / HYA healthy young adult telehealth touchpoint / NEW sparse new patient). Capture as the SQL-comment source of truth at the top of the new clinical seed section in `demo_data.sql`. Every downstream Sprint 12 story consumes this matrix to decide what to seed per patient. Each persona Notes column also names its concrete telehealth use case so the matrix doubles as a demo-script outline. **Implementation:** Persona codes simplified during build to CHR / GER / BH / SUD / HYA / NEW (PSY-S folded into BH, CV-F folded into CHR as cardiac post-event follow-up — see S12-19/S12-20). Matrix lives as a comment block in `07_clinical_data.sql`; later extended to 51 patients in S12-27. (529275f) |
| S12-02   | Sprint 12 | Telehealth appointment category pivot | Seed Data |    M | Done    | High     | Replace the OpenEMR built-in appointment categories currently used in the seed (`Office Visit`, `Established Patient`, `New Patient`, `Behavioral Assessment`, `Preventive Care`) with a clean set of `Zoom`-prefixed telehealth-themed custom categories. Proposed final list: `Zoom Behavioral Health`, `Zoom Cardiology`, `Zoom Chronic Care`, `Zoom MAT (Suboxone)`, `Zoom New Patient`, `Zoom Preventive`, `Zoom Telehealth Visit` — plus rename the existing two from suffix-style to prefix-style for alphabetical grouping (`Telehealth Zoom` → `Zoom Telehealth Visit`, `New Patient Zoom` → `Zoom New Patient`). Rewrites every existing `openemr_postcalendar_events` INSERT to retarget to the new categories per the S12-01 persona matrix (Miller → Behavioral Health; Thompson → Cardiology; CHR/GER → Chronic Care; SUD → MAT; HYA → Preventive; all first-visit appointments → New Patient). All appointment-related rows in `reset.sh` updated. Net effect: every appointment in the demo lives under a `Zoom`-prefixed category so SEs can find them as a single themed group in OpenEMR's category dropdown. **Implementation:** Final categories live in `04_appointment_types.sql` — 5 categories after S12-20 dropped Zoom Cardiology: Primary Care, Behavioral Health, Therapy, Chronic Care, Wellness (the planned MAT/Preventive/New Patient/Telehealth Visit set was further consolidated during S12-17 telehealth-focus tightening). (529275f) |
| S12-03   | Sprint 12 | Insurance company master data         | Seed Data |    S | Done    | High     | Seed ~8 fixed-ID (200–207) rows into `insurance_companies` covering Aetna, BCBS Colorado, United Healthcare, Cigna, Kaiser Permanente, Medicare, Medicaid (CO), Tricare. Reusable target for `insurance_data` FKs in S12-14. Matching `DELETE FROM insurance_companies WHERE id BETWEEN 200 AND 207` added to `reset.sh`. **Implementation:** 8 base payers shipped per plan, in `07_clinical_data.sql`. S12-polish (`19b56d1`) later added 10 regional BCBS plans (MA/NY/PA/GA/FL/NC/CA/KS/TX/IL) on top. (529275f) |
| S12-04   | Sprint 12 | Patient demographic enrichment        | Seed Data |    S | Done    | High     | UPDATE `patient_data` rows 100–129 with race, ethnicity, language, and primary-care provider fields (already partially set via `providerID`). Makes the Demographics panel show real data. No new rows — only column fills on the existing 30 patients. **Implementation:** Demographics enriched as planned in `05_patients.sql`. Later extended in S12-28 to cover the new 130–150 PIDs and address/region redistribution for the original 30. (529275f) |
| S12-05   | Sprint 12 | Historical encounter scaffold         | Seed Data |    S | Done    | High     | One `form_encounter` row per patient dated 30–60 days ago, encounter number drawn from `sequences` per the `UPDATE sequences SET id = id+1; SELECT id` pattern. Acts as the anchor encounter for vitals (S12-10) and labs (S12-11). Provider matches the patient's `providerID`. **Implementation:** Historical encounters shipped in `07_clinical_data.sql`. 47 encounters total after the S12-27 expansion. (529275f) |
| S12-06   | Sprint 12 | Allergies seed                        | Seed Data |    S | Done    | High     | 0–3 `lists` rows (`type='allergy'`) per patient, drawn from a small allergen panel (Penicillin / Sulfa / NSAIDs / Latex / Peanuts / Shellfish / Codeine / NKDA). Includes `reaction` (`hives`/`nausea`/`shortness_of_breath`) and `severity_al` (`mild`/`moderate`/`severe`) matching live `list_options` vocabulary. Drives the Allergies panel. **Implementation:** Allergies seeded in `07_clinical_data.sql` per persona. (529275f) |
| S12-07   | Sprint 12 | Medical problems seed                 | Seed Data |    S | Done    | High     | 1–4 `lists` rows (`type='medical_problem'`) per patient, ICD-10 coded and telehealth-persona-appropriate. Mix includes: `I10` HTN, `E11.9` T2DM, `E78.5` HLD (chronic disease cohort); `F32.1`/`F33.x` MDD, `F41.1` GAD, `F31.81` Bipolar II, `F90.0` ADHD adult (psych + BH-PC); `F11.21` OUD in remission, `Z79.891` long-term opiate analgesic mgmt (SUD); `I25.10` CAD, `I48.91` Afib, `I47.1` SVT (CV-F); `M19.90` OA, `M81.0` Osteoporosis, `E03.9` Hypothyroidism, `N40.0` BPH, `N18.3` CKD3 (GER). Drives the Medical Problems panel and the Past Due Colon/Prostate Cancer Screening reminders. **Implementation:** ICD-10 problems seeded in `07_clinical_data.sql`. 73 problems total after the S12-27 expansion. (529275f) |
| S12-08   | Sprint 12 | Medications seed                      | Seed Data |    M | Done    | High     | 0–6 `lists` rows (`type='medication'`) per patient with a companion `lists_medication` sidecar row each (`usage_category` + `request_intent` per OpenEMR convention). Real generic names with **real RxNorm CUIs**: lisinopril (314076), atorvastatin (617312), metformin (860975), sertraline (313989), escitalopram (321988), bupropion XL (94610), duloxetine (596926), mirtazapine (15996), lamotrigine (28439), methylphenidate ER (847218), **buprenorphine/naloxone 8mg/2mg SL (1010600 — SUD persona)**, levothyroxine (966247), metoprolol succinate (866412), apixaban (1364430), furosemide (310429), etc. Drives the Medications panel. **Implementation:** 49 medication rows + 49 sidecars shipped in `07_clinical_data.sql` with all planned RxNorm CUIs. 70 meds total after the S12-27 expansion. (fd80fd2) |
| S12-09   | Sprint 12 | Active prescriptions seed             | Seed Data |    S | Done    | High     | 1–3 `prescriptions` rows per patient, drawn from the medication list with strength + sig + refills + start_date + provider linkage. Controlled-substance flags on the buprenorphine (Schedule III) and methylphenidate ER (Schedule II) prescriptions; Suboxone gets monthly refill cycles with no auto-refill to match MAT practice. Drives the Prescriptions panel. **Implementation:** 41 prescriptions shipped in `07_clinical_data.sql` with strength/sig/refills/provider linkage; controlled substances correctly `refills=0` per DEA practice. (fd80fd2) |
| S12-10   | Sprint 12 | Vitals seed                           | Seed Data |    M | Done    | High     | One `form_vitals` row per patient (height/weight/BMI/BP/HR/temp/RR/SpO2) attached to the S12-05 historical encounter via a matching `forms` registry row (`formdir='vitals'`). Values tuned to persona — elevated BP for HTN-bearing patients, elevated BMI for CHR/GER, normal across the board for HYA. Drives the Vitals panel and clears the "Past Due Measurement: Weight" clinical reminder. **Implementation:** 29 form_vitals rows + matching forms registry entries shipped. Persona-tuned: HTN cohort 138–152/82–95, BMI 27–33 for CHR/GER, normal across HYA. (fd80fd2) |
| S12-11   | Sprint 12 | Lab results seed                      | Seed Data |    M | Done    | High     | 2–4 lab panels per chronic-disease patient via the four-table chain: `procedure_order` → `procedure_order_code` → `procedure_report` → `procedure_result`. Panels picked by persona: CMP + lipid panel + A1c for CHR; CMP + lipid + BNP for CV-F; TSH for GER and select PSY-S; LFTs for duloxetine patients (drug monitoring); UDS for SUD (MAT compliance); CBC/CMP for stimulant-prescribed PSY-S baseline. LOINC codes (`2093-3` total chol, `2085-9` HDL, `2089-1` LDL, `4548-4` A1c, `3024-7` TSH, `30934-4` BNP, etc.) with fake-but-in-range or annotated-abnormal results. Attached to the S12-05 encounter. Drives the Labs panel. **Implementation:** 10 orders × 10 codes × 10 reports × 24 results shipped. Abnormal flags reflect real clinical pictures (Thomas Walsh A1c 7.8% H, LDL 128 H; Gregory Stone creatinine 1.78 H for CKD3; Samuel Wright ALT 52 H on duloxetine). (fd80fd2) |
| S12-12   | Sprint 12 | Lifestyle + family history seed       | Seed Data |    S | Done    | Medium   | One `history_data` row per patient: tobacco (Current Every Day Smoker / Former / Never per persona), alcohol, exercise, family history flags for cancer/diabetes/HTN/heart disease/stroke. Tobacco status matters most for the smoking-cessation HYA targets and clears the "Past Due Assessment: Tobacco" reminder. **Implementation:** 29 history_data rows shipped; PIDs 125 and 128 are current smokers by design (smoking-cessation telehealth touchpoint targets). Tobacco-assessment reminder clears as planned. (fd80fd2) |
| S12-13   | Sprint 12 | Immunizations seed                    | Seed Data |    S | Done    | Medium   | 2–4 `immunizations` rows per patient with CVX codes: Flu (140) current year, Tdap (115), COVID booster (207), Shingrix (187) for 50+, Pneumovax-23 (33) for 65+. `administered_date` recent. Drives the "Past Due Treatment: Influenza Vaccine" reminder resolution and gives the patient summary a populated immunization history. **Implementation:** 97 immunizations shipped via 5 SELECT-based INSERTs. COVID CVX changed to 208 during implementation. 133 immunizations total after the S12-27 expansion. Past Due Influenza reminder clears. (fd80fd2) |
| S12-14   | Sprint 12 | Insurance assignment per patient      | Seed Data |    S | Done    | Medium   | One `insurance_data` row per patient (`type='primary'`) FK'd to one of the S12-03 companies, distributed across the 8 payers (Medicare for 65+ GER, Medicaid for the SUD patient + select HYA, commercial mix elsewhere). Includes plan_name, policy_number, group_number, subscriber details. Drives the Insurance panel. **Implementation:** 30 primary insurance rows shipped — Medicare for 65+, Medicaid CO for SUD + 2 HYA-young, Tricare for 1, commercial mix elsewhere. 51 insurance rows total after the S12-27 expansion + regional reassignment in S12-polish. (fd80fd2) |
| S12-15   | Sprint 12 | `reset.sh` coverage extension         | Seed Data |    S | Done    | High     | Add `DELETE FROM <table> WHERE pid BETWEEN 100 AND 129` for `lists`, `prescriptions`, `form_vitals`, `history_data`, `immunizations`, `insurance_data`. Add `DELETE FROM lists_medication WHERE list_id IN (SELECT id FROM lists WHERE pid BETWEEN 100 AND 129)` (must run before the `lists` delete). Add `DELETE FROM procedure_order WHERE patient_id BETWEEN 100 AND 129` plus the dependent `procedure_order_code` / `procedure_report` / `procedure_result` rows. Add `DELETE FROM forms WHERE formdir='vitals' AND pid BETWEEN 100 AND 129`. Add `DELETE FROM openemr_postcalendar_categories WHERE pc_catname LIKE 'Zoom %'` (covers the S12-02 categories). Update remaining-row count selects at end of the script. **Implementation:** Coverage landed early during S12-08 to fix a duplicate-accumulation bug. Procedure-chain deletes ordered child-first. PID range extended to 100–150 in S12-29. Reset+seed is idempotent under repeat cycles (counts match exactly across runs). (fd80fd2) |
| S12-16   | Sprint 12 | Per-persona dashboard verification    | QA        |    S | Done    | High     | Manual click-through after seed: pick one representative PID from each persona (e.g. 108 CHR, 102 CV-F, 103 PSY-S, 105 GER, 120 SUD, 101 BH-PC, 121 HYA, 124 NEW), open the OpenEMR patient dashboard, confirm every panel populates appropriately and the Clinical Reminders panel resolves the expected items (Weight, Tobacco, Flu). Confirm appointment calendar shows only `Zoom`-prefixed categories. Document any panels still showing "Nothing Recorded" as backlog. **Implementation:** Verified for representative PIDs: Thomas Walsh (CHR, 5 meds / 3 problems / 3 labs / 4 imm); Andre Dubois (SUD with Suboxone + Medicaid CO); Priya Sharma (HYA with 0 problems by design); Derek Nguyen (NEW, empty except insurance). All panels populate as expected. (fd80fd2) |
| S12-17   | Sprint 12 | Provider + facility + rename matrix design | Seed Data | S | Done    | High | Lock in the 17-provider × 4-facility × 5-patient-rename × 5-new-support-staff assignment as a SQL-comment source of truth in `demo_data.sql`. Drives every Sprint 12 extension story. All providers (existing included) are now Primary Care or Behavioral Health — the demo's telehealth focus tightens to PC + BH. **East=10** (OConnor 10, Rodriguez 11, Miller 12, Chen 16, Eriksson 17, Yuki Tanaka 18, Garcia 19, Amy Martin 21, Lucas Johnson 22, Lisa Patel 25), **Mountain=4** (Nelson 14, Priya Patel 15, Hiroshi Tanaka 26, David Thompson 27), **West=2** (Marcus Thompson 13, Anderson 23), **Central=1** (Joe Smith 24). Each facility also gets exactly 1 nurse + 1 MA (5 new support staff required). Patient renames: Linda Patel→Whitaker, Maria Chen→Wong, Aisha Johnson→Carter, Yuki Tanaka→Sato, Isabelle Martin→Vasquez. **Implementation:** Matrix shipped as the SQL-comment source of truth distributed across the relevant sections of the split files (03_staff, 02_facilities, 05_patients). Per-facility distribution as planned. (e52e654) |
| S12-18   | Sprint 12 | Facility expansion (East + West + Central) | Seed Data | S | Done    | High     | Add facility id=2 ("Zoomly Medical Center East", 25 Cambridge St, Boston MA 02114, Eastern TZ), id=4 ("Zoomly Medical Center West", 200 Parnassus Ave, San Francisco CA 94143, Pacific TZ), and id=5 ("Zoomly Medical Center Central", 456 Truman Road, Kansas City MO 64106, Central TZ). Existing facility id=1 (Denver, Mountain TZ) keeps its name + address. Reset.sh extended to clear ids 2, 4, 5. **Implementation:** Final facility IDs renumbered in S12-polish to 1=Denver/CO, 2=Boston/MA, 3=SF/CA, 4=Kansas City/MO (claims id=3 slot so OpenEMR's stock default no longer fills it). Plus 4 regional pharmacies added in `02_facilities.sql`. (e52e654, refined in 19b56d1) |
| S12-19   | Sprint 12 | Marcus Thompson specialty pivot       | Seed Data |    S | Done    | High     | UPDATE `users` id=13 — specialty `Cardiology` → `Internal Medicine`, taxonomy `207RC0000X` → `207R00000X`. His existing CV-F patients keep their cardiac dx (CAD, MI, afib, SVT, MVP, PVCs) — those are real conditions a PCP legitimately co-manages for stable post-event follow-up. Only the provider label changes. **Implementation:** Updated inline in his PROVIDERS row in `03_staff.sql`. Patients' cardiac dx retained. (e52e654) |
| S12-20   | Sprint 12 | Drop "Zoom Cardiology" category       | Seed Data |    S | Done    | High     | DELETE the `Zoom Cardiology` row from `openemr_postcalendar_categories`. UPDATE every `openemr_postcalendar_events` and `form_encounter` row that pointed at `@zoom_cardiology_catid` to use `@zoom_chronic_care_catid` instead. Sync `pc_title='Zoom Chronic Care'`. Reset.sh `LIKE 'Zoom %'` clause already covers it. Net effect: 5 telehealth categories remain (Behavioral Health, Chronic Care, MAT, New Patient, Preventive). **Implementation:** Cardiology row removed; `@zoom_cardiology_catid` variable removed; S12-02 retarget UPDATE #3 removed; catch-all UPDATE #7 now sweeps Thompson's appointments to Chronic Care; pc_title sync removed; S12-05 Thompson encounters retargeted. Final 5 categories: Primary Care, Behavioral Health, Therapy, Chronic Care, Wellness. (e52e654) |
| S12-21   | Sprint 12 | 12 new provider records + 5 new support staff | Seed Data | M | Done    | High     | INSERT 12 new providers (ids 14-19, 22-27) into `users`, `users_secure`, `gacl_aro`, `groups` (Physicians), `gacl_groups_aro_map`. Plus 5 new support staff (ids 32-36) — nurses Sarah Martinez (32, East), Ken Watanabe (33, Central), Maria Rodriguez (34, West); medical assistants Emma Wilson (35, Central), Cheryl Lewis (36, West) — into the same tables with `Clinicians` group. Usernames `first.last` format. Email `first.last@example.org`. Password hash matches existing ZoomDem0! across all new users. Provider NPIs 1234567894–1234567905. Specialty mix (providers): 7 Internal Medicine, 6 Family Medicine, 2 Psychiatry, 1 LCSW (Yuki Tanaka), 1 Addiction Medicine (Lucas Johnson), 1 Psychiatric NP (Priya Patel), 1 Family NP (Amy Martin promoted). **Implementation:** Username format updated during S12-polish from `first.last` to `first-initial+lastname` (jnelson, ppatel, etc.) — provides shorter, more demoable login names. Each user has a unique inline bcrypt hash. All 12 providers + 5 support staff shipped in `03_staff.sql`. (e52e654, refined in 19b56d1) |
| S12-22   | Sprint 12 | Amy Martin RN → FNP promotion         | Seed Data |    S | Done    | Medium   | UPDATE `users` id=21 — `abook_type='nurse'` → `'physician'`, `authorized=0` → `1`, `calendar=0` → `1`, `specialty='Nursing'` → `'Family Medicine'`, `taxonomy='163W00000X'` → `'363LF0000X'`, add NPI 1234567906, `physician_type='FNP'`, `facility_id=2` (East). Move from `Clinicians` group to `Physicians`. **Implementation:** Promoted inline in PROVIDERS section of `03_staff.sql`; group reassignment from Clinicians (12) to Physicians (13) shipped. (e52e654) |
| S12-23   | Sprint 12 | Provider + support-staff facility distribution | Seed Data | S | Done    | High     | UPDATE `users.facility_id` per the S12-17 matrix. Providers: Mountain (id=1) → 14, 15, 26, 27; East (id=2) → 10, 11, 12, 16, 17, 18, 19, 21, 22, 25; West (id=4) → 13, 23; Central (id=5) → 24. Support staff: Mountain keeps Lee (20) + Williams (30); East gets new nurse Martinez (32) + Song (31, redistributed from Mountain); Central gets Watanabe (33) + Wilson (35); West gets Rodriguez (34) + Lewis (36). Each facility ends with exactly 1 nurse + 1 MA. **Implementation:** Distribution baked into the INSERT rows directly (no separate UPDATE block). Per-staff `users_facility` rows added so calendar scoping works correctly under `login_into_facility=1`. (e52e654) |
| S12-24   | Sprint 12 | Patient renames (5 PIDs)              | Seed Data |    S | Done    | High     | UPDATE `patient_data` for the 5 PIDs whose names collided with new provider usernames: 105 Linda Patel→Whitaker, 107 Maria Chen→Wong, 109 Aisha Johnson→Carter, 111 Yuki Tanaka→Sato, 125 Isabelle Martin→Vasquez. Update `email` to match new last name. PID 105 also gets `patient_access_onsite.portal_username` + `patient_data.cmsportal_login` updated since she has portal access. **Implementation:** Renames done inline in the `patient_data` INSERT rows in `05_patients.sql`; emails updated; PID 105's portal_username + cmsportal_login replaced (replace_all). (e52e654) |
| S12-25   | Sprint 12 | reset.sh extension for new users + facilities | Seed Data | S | Done    | High | Extend `users` / `users_secure` / `gacl_aro` / `groups` / `gacl_groups_aro_map` / `facility` DELETE WHERE clauses to cover provider ids 14-19, 22-27 + support ids 32-36 + facility ids 2, 4, 5. Verify reset+seed remains idempotent under repeat cycles. **Implementation:** Reset.sh extended as planned. S12-polish later added `gacl_groups_aro_map` clearing for Front Office assignments and `users_facility` cleanup. Reset+seed verified idempotent (counts identical across runs). (e52e654, refined in 19b56d1) |
| S12-26   | Sprint 12 | Per-facility verification             | QA        |    S | Done    | High     | Run reset+seed twice, confirm 17 providers exist with correct facility_id distribution (East=10, Mountain=4, West=2, Central=1). Confirm 4 nurses + 4 MAs distributed one-per-facility. Confirm all 5 patient renames applied + portal_username updated for PID 105. Confirm no patient name collides with any user username. Confirm `Zoom Cardiology` is gone and Thompson's appointments now show `Zoom Chronic Care`. Confirm pytest still passes. **Implementation:** All checks passed. Pytest 282/282 after one test update (`test_seed_data_includes_patient_portal_credentials_for_demo_patients` expected updated `linda.whitaker` username). (e52e654) |
| S12-27   | Sprint 12 | 51-patient × 17-provider × 4-facility matrix | Seed Data | S | Done    | High | Lock in the "3 patients per provider" allocation matrix as a SQL-comment source of truth. 30 existing patients keep their PIDs; 21 new patients get PIDs 130–150. Persona deltas: +2 PSY-S (with Eriksson + Priya Patel), +2 SUD (with Lucas Johnson), +6 HYA, +4 CHR, +2 GER, +2 BH-PC, +3 NEW (one per non-Mountain facility — Mountain already has 124). Per-provider assignment respects persona/specialty constraints: Miller/Eriksson see PSY-S; Yuki Tanaka sees BH-PC; Lucas Johnson sees SUD; Priya Patel sees PSY-S; everyone else is a PCP. Drops the "Default Facility (Unused)" facility id=3 row entirely (DELETE instead of inactive+rename) since nothing references it after admin moves to facility id=1. **Implementation:** 51-patient × 17-provider × 4-facility matrix captured in the persona-matrix comment block. Default Facility (id=3) dropped during seed; reset.sh recreates the stock row via `INSERT IGNORE` so post-reset matches a fresh OpenEMR install. (896b936) |
| S12-28   | Sprint 12 | 21 new patient records with full clinical data | Seed Data | L | Done    | High | INSERT 21 new patients (PIDs 130–150) into `patient_data` with region-appropriate addresses, phone area codes, emails, and `providerID` set to their assigned provider. Full clinical data per the existing pattern: demographic enrichment (race/ethnicity/occupation/contact), historical encounters (skip 3 NEW personas — 134/147/148), allergies, medical problems (ICD-10), medications + sidecars (real RxNorm CUIs), prescriptions, vitals + matching forms row (skip NEW), labs for chronic-disease patients, history_data (skip NEW), age-gated immunizations (Flu/Tdap/COVID/Shingrix/Pneumovax), insurance assignments. Also updates existing 30 patients' addresses/phones/providerIDs inline in their original `patient_data` INSERT rows to match the new facility distribution (East cities for 17 patients, Mountain cities for 8 redistributed across Denver/Boulder/COS/SLC, West cities for 5). **Implementation:** 21 new patients shipped with full clinical data via existing SELECT-based INSERTs (BETWEEN 100 AND 150). Existing 30 patients moved from Denver to facility regions; insurance further refined in S12-polish with 10 regional BCBS plans. (896b936, refined in 19b56d1) |
| S12-29   | Sprint 12 | Appointment + historical encounter retarget | Seed Data | M | Done    | High | UPDATE `form_encounter.provider_id` for the existing 29 historical encounters to match each patient's new `providerID`. Single JOIN-based UPDATE for the existing 112 `openemr_postcalendar_events` rows retargets `pc_aid` to the patient's new providerID. Add ~63 new appointments for the 21 new patients (3 per new patient over the existing 14-day window, persona-appropriate Zoom category). Reset.sh PID range extended to 100–150 and `lists_medication` subquery + `procedure_*` subqueries updated to match. Verify reset+seed remains idempotent (counts identical across cycles); pytest still passes. **Implementation:** Single JOIN-based UPDATE retargets all 112 existing appointments. 63 new appointments added. Final: 175 appointments + 47 encounters + 47 vitals. Reset.sh fix: appointment DELETE scope changed from `pc_aid IN ('10','11','12','13')` to `pc_pid BETWEEN 100 AND 150` (old form left new providers' appointments orphaned). Two reset+seed cycles yield identical counts. Pytest 282/282 after one test update (`test_seed_data_uses_reserved_patient_email_addresses` expected count 30 → 51). (896b936) |
| S12-30   | Sprint 12 | Seed file split (post-plan refactor)  | Seed Data |    S | Done    | High     | Not in original S12-01..29 plan — added during implementation. Single 2359-line `demo_data.sql` broken into 7 ordered files (`01_globals.sql` → `07_clinical_data.sql`) so each concern can be edited in isolation. **Implementation:** `seed.sh` cat-pipes `0[1-7]_*.sql` into one mariadb connection so session vars (`@apt_base`, `@zoom_*_catid`, etc.) persist across file boundaries. `test_seed_data_sql.py::_demo_sql_text()` reads the 7-file concatenation. Original file removed. Counts unchanged after split (51 patients / 175 appts / 47 encs / 73 problems / 70 meds / 51 ins / 133 imm). (af6c6e8) |
| S12-31   | Sprint 12 | Weekday-only appointments             | Seed Data |    S | Done    | Medium   | Not in original plan — added in response to demo feedback that appointments landed on weekends. **Implementation:** `06_appointments.sql` precomputes `@day1..@day14` as the next 14 weekday dates starting tomorrow (Sat/Sun seed runs shift `@apt_base` to the following Monday). Seeded appointment INSERTs/reference-generated appointments use `@dayN` instead of `DATE(DATE_ADD(NOW(), INTERVAL N DAY))`. Math: `+N-1` calendar days plus `FLOOR((N-1 + WEEKDAY(@apt_base)) / 5) * 2` extra days for each crossed weekend. Verified zero Sat/Sun appointments across seeded rows. (cd01b15) |
| S12-32   | Sprint 12 | Sprint 12 polish (facility-scoped logins, regional refresh) | Seed Data | S | Done    | High     | Not in original plan — added during demo-prep validation. **Implementation:** Five things in one drop: (1) `globals.login_into_facility=1` so providers see only their own facility's calendar; (2) facility renumber to 1=CO / 2=MA / 3=CA / 4=MO claims the id=3 slot OpenEMR's stock default uses; (3) regional pharmacies + addresses added to `02_facilities.sql`; (4) 25 unique inline bcrypt hashes per user with `first-initial+lastname` username format; full staff details (DEA, tax ID, UPIN, state license, weno, supervisor, billing facility, info); per-staff `users_facility` rows for calendar scoping; (5) 10 regional BCBS plans (MA/NY/PA/GA/FL/NC/CA/KS/TX/IL); regional reassignment per patient state; regex guard on `pc_pid` for facility/provider retarget UPDATEs handles OpenEMR system events with empty pc_pid; (6) `reset.sh` clears `gacl_groups_aro_map` (fixes lingering Front Office assignments persisting across resets), extends addresses/insurance ranges to cover new rows, adds pharmacies + `users_facility` cleanup, recreates stock facility id=3 via `INSERT IGNORE`. (19b56d1) |

## Sprint 13 - SE Demo Completion And V1 Readiness  *(current sprint)*

Reframed from the deferred Sprint 9. Sprint 12 built the demo dataset; Sprint 13 takes the integration through end-to-end SE rehearsal against that data, closes the eSign loop, and ships the demo-script docs Josh has been wanting before declaring V1.

| ID     | Area          | Size | Status  | Priority | Story / Task                             | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------ | ------------- | ---: | ------- | -------- | ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S13-01 | QA            |    L | Done    | High     | SE demo run-through (was S9-01)          | **Done.** Full end-to-end exercised against the seeded dataset: scheduling → waiting room → meeting start → clinical note → SOAP + clinical-note writeback verified for at least one PID per persona (CHR / GER / BH / SUD / HYA / NEW). |
| S13-02 | OpenEMR       |    M | Done    | High     | Wire `complete_zoom_note` to eSign (was S9-02) | **Done** (commit `feda047`). eSign event wired in `openemr/patches/clinical_note_fetcher/forms.php` for both form-level (SOAP / Clinical Notes) and encounter-level eSigns. Uses `window.location.origin` to dodge the 301 redirect. PHP proxy dropped its `clinical:notes:write` ACL check (mismatched demo provider permissions); session auth + HMAC signing to Flask are sufficient. `complete_zoom_note` Flask route now audits every skip/error path with `detail.reason`. |
| S13-03 | Flask Backend |    M | Done    | Medium   | Locked/eSigned encounter guard (was S9-03)     | **Done** (commit `feda047`). `encounter_lock_target()` helper checks `esign_signatures` for both `form_encounter`-level and `forms`-level (SOAP / Clinical Notes) locks. `write_note_to_encounter()` and the `fetch_zoom_note` route refuse on hit and emit `note.write_skipped_locked` / `note.manual_fetch_failed` (`reason=encounter_locked`) with the `lock_target` in detail. Fetch endpoint returns 409. `forms.php` adds render-time SQL guards so the Retrieve Zoom Note button is hidden on locked encounters AND on non-Zoom-linked encounters (commit `b357fc5`). |
| S13-04 | QA            |    M | Done    | Medium   | Demo script documentation (was S9-04)          | **Done.** Step-by-step SE walkthrough captured, keyed to the Sprint 12 personas (CHR / GER / BH / SUD / HYA / NEW) so demoers can pick a persona and follow the same script. |
| S13-05 | Flask Backend |    S | Done    | Medium   | Verify waiting-room webhook actually fires     | **Done**. Resolved by subscribing to `meeting.participant_jbh_waiting` (the patient-arrived signal that actually fires before the host joins under our meeting settings — despite the "JBH waiting" name suggesting otherwise). `meeting.participant_joined_waiting_room` only fires *after* the host has joined and admitted the patient through the waiting room, so it doesn't drive any new lifecycle state. The dispatcher at `zoom_webhook.py:121` still routes both events to the same handler; the `participant_joined_waiting_room` branch is tagged with a TODO and slated for removal after more demo testing confirms it's never needed. Subscription guidance updated in `implementation-setup-guide.md`. |
| S13-09 | Seed Data     |    M | Done    | High     | 100+ patient panel expansion                   | Expanded the seed from 72 to 108 patients (PIDs 100-207), giving each of the 18 patient-panel providers 6 patients. Added 36 realistic regional panel patients, 108 additional weekday appointments, persona-driven demographics/clinical artifacts, complete insurance/contact/care-team coverage across the full patient range, reset cleanup for PIDs 100-207, seed tests for the 108-patient range and 6-per-provider invariant, and docs current-state updates. |
| S13-06 | QA            |    S | Backlog | High     | Pre-V1 regression sweep                        | After S13-01..05 land: full `pytest`, full `docker compose down -v && docker compose build && start-dev.sh && alembic upgrade head && seed_data/reset.sh && seed_data/seed.sh` from a clean state, verify all four facilities, all 18 patient-panel providers, all 108 patients, all 292 appointments come up correctly and the dashboard panels for one PID per persona populate as documented. Also run a `start-dev.sh --baked` pass to confirm the baked OpenEMR image works without the dev override. |

> Current seed-data note: the seed now covers PIDs 100-207 (108 patients, 6 per patient-panel provider). The S13/S14 hardening pass fills fake plain-digit SSNs, home phone, trusted email, regional pharmacy, required insurance subscriber fields, regional payers, provider+nurse care teams, and panel-expansion clinical artifacts; `reset.sh` clears the full patient range and corresponding insurance/care-team rows.

## Tech Debt / Architecture Backlog (unscheduled)

Items here are known limitations or refactors that aren't tied to a sprint yet. Promote into a sprint when the cost of *not* doing the work starts to dominate.

| ID    | Area          | Size | Status  | Priority | Story / Task                              | Notes                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | ------------- | ---: | ------- | -------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TD-01 | Flask Backend |    L | Backlog | Medium   | Recurring-meeting / shared Zoom meeting support | `MeetingRecord.zoom_meeting_id` is the string PK, which forces 1 MeetingRecord per Zoom meeting and breaks if a single Zoom meeting is reused across multiple appointments/encounters (recurring telehealth, shared room links). Fix requires: (1) surrogate integer PK on `MeetingRecord` with `(zoom_meeting_id, openemr_appointment_id)` uniqueness; (2) retarget FKs from `MeetingPatient` and `ClinicalNoteRecord`; (3) add `openemr_appointment_id` (and ideally `openemr_encounter_number`) to `ClinicalNoteRecord` so retrieves can disambiguate by appointment; (4) sweep every `MeetingRecord.query.filter_by(zoom_meeting_id=...)` callsite to include appointment context. Tactical mitigation in place today: `MeetingRecord.clinical_note` `order_by="ClinicalNoteRecord.received_at.desc()"` makes the single-appointment-multiple-notes case deterministic. |
| TD-02 | OpenEMR Upstream |    M | Backlog | Low      | Submit upstream PR for RsaSha384Signer multi-client JWT fix | Our `openemr/patches/RsaSha384Signer.php` (S7-08) ships a minimal kid-extraction fix as a downstream bind mount. The upstream-quality fix would also: (1) delete `$this->headers` and `modifyHeader()` — vestigial Lcobucci `Signer` interface remnants for signing, but this class is verify-only and throws on `sign()`; (2) tighten `JsonWebKeySet::getJSONWebKey()` to reject null kid when multiple keys exist instead of silently returning the first match; (3) add PHPUnit coverage under `tests/Tests/Unit/...` for kid extraction + multi-key JWKS lookup; (4) file an OpenEMR GitHub issue calling out the multi-tenant security implication (client A could authenticate as client B if A's key happened to be first in the JWKS array). Once merged + released, drop our downstream patch, the corresponding COPY line in `openemr/Dockerfile`, and the bind-mount line in `docker-compose.override.yml(.example)`. |
| TD-03 | Database     |    M | Partially done | Medium   | Enforce strict 1:1 OpenEMR provider ↔ Zoom user mapping globally | `provider_mappings` has been renamed to `user_mappings`, `openemr_provider_id` to `openemr_user_id`, role flags were added, and `allow_shared_zoom_user` was removed. Current remaining risk is validating the exact active partial-unique behavior across provider and ZCC-agent roles. |
